# v0.1 完善计划

**开始时间:** 2026-06-26
**完成时间:** 2026-06-26
**提交:** 835aedb

## ✅ P1-1: 程序化像素精灵图
使用 Godot `Image` API 运行时绘制像素角色:
- `scripts/systems/pixel_sprite_generator.gd` — 绘制工具类
- 玩家忍者: 6帧 (idle, run1, run2, jump, attack, hurt) — 深蓝+银色 blade
- Shadow Crawler: 5帧 (idle, walk1, walk2, attack, hurt) — 棕色+黄色眼睛
- Wraith: 5帧 (idle, move1, move2, attack, hurt) — 紫色+绿色眼睛+发光效果
- 基本绘图: `_draw_pixel`, `_draw_rect`, `_draw_circle`

## ✅ P1-2: AnimatedSprite2D + 动画
- 替换所有 ColorRect 为 AnimatedSprite2D
- player_controller.gd: `_setup_sprite()` + `_update_animation()` 状态机驱动的动画切换
- base_enemy.gd: 虚拟 `_setup_sprite()` + `_update_animation()` + flip_h
- ShadowCrawler/Wraith: 各自重写 `_setup_sprite()` 加载专属精灵表

## ✅ P1-3: 二段跳跃
- player_controller.gd: `_double_jump_used` 标记
- 空中再按 jump → `velocity.y = stats.jump_velocity * 0.8`
- 落地 → `_double_jump_used = false`

## ✅ P1-4: 暂停菜单
- `scripts/ui/pause_menu.gd` — Control + `PROCESS_MODE_ALWAYS` + `_input` 处理
- `scenes/ui/pause_menu.tscn` — 半透明遮罩 + "继续"/"返回主菜单"
- HUD 中实例化 PauseMenu
- hud.gd 移除 pause 处理

## 验证状态
- [x] 角色显示像素精灵（非色块）
- [x] 状态切换时动画正确播放
- [x] 可以在空中跳两次
- [x] Esc 弹出暂停菜单
